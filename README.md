# Engineering Crisis Game

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```

Use public links after deployment:
- Host/control: `?view=host`
- Student play: `?view=play`
